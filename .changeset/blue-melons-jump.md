---
"@nozomioai/nia": patch
---

Improve CLI compatibility and experimental API behavior across auth, usage, oracle, and sources commands. This includes persisting the `--experimental` API preference, normalizing usage output across multiple response shapes, and improving `nia sources resolve` multi-match output with clearer IDs and `nia search query` follow-up guidance.
